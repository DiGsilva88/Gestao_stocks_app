<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="24dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Editar Perfil"
                android:textColor="@color/text_primary"
                android:textSize="24sp"
                android:textStyle="bold" />

            <ImageButton
                android:id="@+id/btnFechar"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/bg_campo"
                android:src="@android:drawable/ic_menu_close_clear_cancel"
                android:contentDescription="Fechar"
                app:tint="@color/text_primary" />

        </LinearLayout>

        <TextView
            style="@style/RotuloCampo"
            android:labelFor="@id/editNome"
            android:text="NOME" />

        <EditText
            android:id="@+id/editNome"
            style="@style/CampoTexto"
            android:inputType="textPersonName" />

        <TextView
            style="@style/RotuloCampo"
            android:labelFor="@id/editEmail"
            android:text="UTILIZADOR" />

        <EditText
            android:id="@+id/editEmail"
            style="@style/CampoTexto"
            android:enabled="false"
            android:textColor="@color/text_muted"
            android:inputType="text" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:text="Alterar password"
            android:textColor="@color/text_primary"
            android:textSize="16sp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Deixa os campos em branco para manter a password atual."
            android:textColor="@color/text_muted"
            android:textSize="12sp" />

        <TextView
            style="@style/RotuloCampo"
            android:labelFor="@id/editPasswordAtual"
            android:text="PASSWORD ATUAL" />

        <EditText
            android:id="@+id/editPasswordAtual"
            style="@style/CampoTexto"
            android:inputType="textPassword" />

        <TextView
            style="@style/RotuloCampo"
            android:labelFor="@id/editPasswordNova"
            android:text="NOVA PASSWORD" />

        <EditText
            android:id="@+id/editPasswordNova"
            style="@style/CampoTexto"
            android:inputType="textPassword" />

        <TextView
            style="@style/RotuloCampo"
            android:labelFor="@id/editPasswordConfirmar"
            android:text="CONFIRMAR NOVA PASSWORD" />

        <EditText
            android:id="@+id/editPasswordConfirmar"
            style="@style/CampoTexto"
            android:inputType="textPassword" />

        <Button
            android:id="@+id/btnGuardar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:text="Guardar alterações"
            android:backgroundTint="@color/accent"
            android:textColor="@color/contrast" />

    </LinearLayout>

</ScrollView>
<?xml version="1.0" encoding="utf-8"?>
<!--
    Nada da aplicação sai do dispositivo. O allowBackup="false" do manifesto
    trata das versões anteriores ao Android 12; a partir daí é este ficheiro
    que manda, e a transferência dispositivo-a-dispositivo ignora o
    allowBackup, por isso tem de ser excluída à parte.

    O que estaria em jogo: stockify.db (contas e todo o stock), as
    SharedPreferences da sessão e as fotos dos produtos em files/produtos.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
